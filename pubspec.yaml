name: cutcitos
description: "Aplicación Cutcitos - Conectando estudiantes vendedores"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'  # Más específico para evitar futuros conflictos

dependencies:
  flutter:
    sdk: flutter

  # Paquetes principales (versiones actualizadas)
  cupertino_icons: ^1.0.8
  intl_phone_field: ^3.2.0
  flutter_dotenv: ^5.1.0  # Versión más reciente
  http: ^1.2.0
  flutter_secure_storage: ^8.0.0

  # Agrega estos paquetes útiles para autenticación:
  provider: ^6.1.1       # Para gestión de estado
  shared_preferences: ^2.2.2  # Para almacenamiento local

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  palette_generator: ^0.3.3
  intl: ^0.18.1

flutter:
  uses-material-design: true

  assets:
    - assets/cutcitos.png
    - assets/images/caramelos.jpg
    - assets/images/chocolates.jpg
    - assets/images/chocolates2.jpg
    - assets/images/gomitas1.jpg
    - assets/images/paletas.jpg
    - assets/images/panditas.jpg
    - assets/images/profile.jpg
    - assets/images/default_profile.jpg
    - assets/images/default_category.jpg
    - assets/images/products/
    - assets/images/default-product.png
    - assets/images/default_product.jpg
    - .env               # Incluye el archivo .env como asset